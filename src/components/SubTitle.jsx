import React from "react";

const SubTitle = ({ text }) => {
    return (
        <h3 className="text-2xl mt-5 font-semibold font-RobotoMono leading-7 text-primary">
            {text}
        </h3>
    );
};

export default SubTitle;
